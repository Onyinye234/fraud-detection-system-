spring:
  application:
    name: fraud-detection-service

  datasource:
    url: jdbc:mysql://localhost:3307/fraud_detection
    username: root
    password: Ayitu023113

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true